#config.yaml
program: train_model_arg.py
method: bayes
metric:
  name: train_loss
  goal: minimize
parameters:
  learning_rate:
    min: 0.0001
    max: 0.0003
  optimizer:
    values: ["adam", "sgd"]